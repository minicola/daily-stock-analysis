# Stock Portfolio Report
1. Use DataFetcherManager (NOT UnifiedDataProvider) to fetch real-time data
2. If any API call fails, report the failure — do NOT substitute fake data
3. Output as markdown to reports/YYYY-MM-DD.md
